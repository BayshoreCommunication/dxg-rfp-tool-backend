import crypto from"node:crypto";import{v7 as uuidv7}from"uuid";import type{ContextFixture}from"./domain";
export const deterministicContextCandidate=(proposalId:string,fixture:ContextFixture)=>{if(fixture==="invalid-output")return{invalid:true};const sourceVersionId=`fixture:${fixture}:v1`,values=fixture==="synthetic-conference-simple"?[{path:"/content/event/eventName",value:"Synthetic DXG Conference"},{path:"/content/event/eventFormat",value:"In-Person"}]:fixture==="prompt-injection"?[]:[{path:"/content/event/eventName",value:"Synthetic DXG Leadership Conference"},{path:"/content/event/eventFormat",value:"Hybrid"},{path:"/content/event/eventObjectives",value:"Bring leaders together for learning and collaboration."},{path:"/content/venueSchedule/numberOfEventRooms",value:"6"}];const evidence=values.map((_,i)=>{const id=uuidv7(),fragmentId=`fixture-fragment-${i+1}`,contentChecksum=crypto.createHash("sha256").update(`${sourceVersionId}:${fragmentId}`).digest("hex");return{id,sourceVersionId,fragmentId,locator:{fixture,line:i+1},contentChecksum};});const candidates=values.map((x,i)=>({path:x.path,value:x.value,evidence:[{sourceVersionId,fragmentId:evidence[i].fragmentId}],confidence:0.99,state:"pending",validation:{valid:true}}));return{patch:{schemaVersion:"proposal-extraction-patch.v1",proposalId,proposalVersion:1,sourceVersionIds:[sourceVersionId],candidates},evidence,issues:fixture==="prompt-injection"?[{code:"PROMPT_INJECTION_IGNORED",severity:"blocking",paths:[]}]:fixture==="synthetic-conference-medium"?[{code:"MISSING_SHOW_END_TIME",severity:"question",paths:["/content/venueSchedule/showEndTime"]}]:[]};};

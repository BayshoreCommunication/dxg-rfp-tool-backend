import{withPostgresTransaction}from"../../../config/postgres";import{ProposalContextError}from"./domain";
export const latestProposalContextRunId=(input:{organizationMongoId:string;actorUserMongoId:string;proposalMongoId:string})=>withPostgresTransaction(async c=>{await c.query("SELECT set_config('app.organization_mongo_id',$1,true)",[input.organizationMongoId]);const org=await c.query<{id:string}>("SELECT id FROM rfpilot.organizations WHERE external_mongo_id=$1 AND status='active'",[input.organizationMongoId]);if(!org.rows[0])throw new ProposalContextError("ORGANIZATION_NOT_READY","Organization data foundation is unavailable.",503);await c.query("SELECT set_config('app.organization_id',$1,true)",[org.rows[0].id]);const result=await c.query<{id:string}>(`SELECT r.id FROM rfpilot.proposal_context_runs r JOIN rfpilot.proposal_references p ON p.id=r.proposal_reference_id JOIN rfpilot.users u ON u.id=p.owner_user_id WHERE p.external_mongo_id=$1 AND u.external_mongo_id=$2 AND r.status='succeeded' AND r.retention_until>now() ORDER BY r.created_at DESC LIMIT 1`,[input.proposalMongoId,input.actorUserMongoId]);if(!result.rows[0])throw new ProposalContextError("CONTEXT_RUN_UNAVAILABLE","No completed context run is available.",404);return result.rows[0].id;});

import type{DraftSection}from"./domain";
const text=(v:unknown)=>typeof v==="string"&&v.trim()?v.trim():null;
export const deterministicProposalDraft=(proposal:{event?:Record<string,unknown>;venueSchedule?:Record<string,unknown>})=>{const event=proposal.event??{},venue=proposal.venueSchedule??{},name=text(event.eventName),format=text(event.eventFormat),objectives=text(event.eventObjectives),rooms=text(venue.numberOfEventRooms);const sections:DraftSection[]=[],gaps:Array<{code:string;paths:string[]}>=[];if(name||format)sections.push({key:"event_overview",heading:"Event Overview",paragraphs:[{text:`${name??"The event"} is planned${format?` as a ${format.toLowerCase()} experience`:""}.`,evidencePaths:[...(name?["/content/event/name"]:[]),...(format?["/content/event/format"]:[])]}]});else gaps.push({code:"MISSING_EVENT_OVERVIEW",paths:["/content/event/name","/content/event/format"]});if(objectives)sections.push({key:"objectives_audience",heading:"Objectives and Audience",paragraphs:[{text:objectives,evidencePaths:["/content/event/objectives"]}]});else gaps.push({code:"MISSING_EVENT_OBJECTIVES",paths:["/content/event/objectives"]});if(rooms)sections.push({key:"venue_schedule",heading:"Venue and Schedule Summary",paragraphs:[{text:`The current plan includes ${rooms} event rooms.`,evidencePaths:["/content/venueSchedule/roomCount"]}]});else gaps.push({code:"MISSING_ROOM_COUNT",paths:["/content/venueSchedule/roomCount"]});if(!text(event.startDate)||!text(event.endDate))gaps.push({code:"MISSING_EVENT_DATES",paths:["/content/event/startDate","/content/event/endDate"]});return{schemaVersion:"proposal-draft.v1",sections,gaps};};

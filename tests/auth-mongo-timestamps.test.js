const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path");
test("timestamped user upserts leave createdAt and updatedAt to Mongoose",()=>{const root=path.resolve(__dirname,".."),files=["src/modules/auth/infrastructure/mongo/mongoAuthAccountRepository.ts","src/modules/auth/infrastructure/mongo/mongoGoogleAccountRepository.ts"];for(const file of files){const source=fs.readFileSync(path.join(root,file),"utf8");for(const block of source.matchAll(/\$setOnInsert:\s*\{([\s\S]*?)\n\s*\}/g)){assert.doesNotMatch(block[1],/\bcreatedAt\s*:/,`${file} duplicates createdAt`);assert.doesNotMatch(block[1],/\bupdatedAt\s*:/,`${file} duplicates updatedAt`);}}});
